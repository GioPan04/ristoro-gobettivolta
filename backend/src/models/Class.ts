export default class StudentsClass {
    public id: number;
    public className: string;

    constructor(
        id: number,
        className: string,
    ) {
        this.id = id;
        this.className = className;
    }
}